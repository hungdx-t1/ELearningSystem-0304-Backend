namespace ELearning.Core.Enums;

public enum UserRole
{
    Admin,
    Instructor,
    Student
}

public enum LessonType
{
    Video,
    Document,
    Quiz
}

public enum VideoProvider
{
    Youtube,
    LocalUpload
}